# Raw payload too large for git (177 MB, exceeds GitHub's 100 MB blob limit).
# Retrieval is fully specified in data/raw/PROVENANCE.json and its SHA256 is
# pinned in DATA_MANIFEST.json; src/fetch_data.sh re-downloads it verbatim.
data/raw/battledim_official/L-TOWN_v2_Real.inp
data/processed/**/*.csv
__pycache__/
*.pyc

# EPANET scratch files written into cwd by wntr's EpanetSimulator
temp.bin
temp.inp
temp.rpt
temp*.bin
[A-Za-z][A-Za-z][A-Za-z][A-Za-z][A-Za-z][A-Za-z][A-Za-z][A-Za-z]
!src/**
!logs/**
