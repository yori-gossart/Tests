# Raw payload too large for git (177 MB, exceeds GitHub's 100 MB blob limit).
# Retrieval is fully specified in data/raw/PROVENANCE.json and its SHA256 is
# pinned in DATA_MANIFEST.json.
data/raw/battledim_official/L-TOWN_v2_Real.inp

# Regenerated SCADA: large, and reproducible from src/regenerate_scada.py.
# Per-file SHA256 is recorded in REGENERATION_METADATA.json, which IS tracked.
data/processed/**/*.csv
data/processed/*.npz

__pycache__/
*.pyc

# Scratch files that wntr's EpanetSimulator writes into the working directory:
# a fixed "temp.*" set plus randomly named 8-character extensionless files.
temp.bin
temp.inp
temp.rpt
/????????
